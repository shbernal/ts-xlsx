// oxlint configuration. Replaces the linter half of the biome.json this repo used to
// carry; oxfmt owns layout, and the two never overlap.
//
// JSONC because the choices below are measured, not preferred, and the measurements have
// to survive contact with the next person who wonders why the config is not simply
// `"categories": {"all": "error"}`. oxlint auto-discovers `.oxlintrc.jsonc` as it does
// `.oxlintrc.json`.
//
// THIS FILE MUST STAY AT THE REPO ROOT. `ignorePatterns` and every `overrides.files`
// glob are resolved relative to the directory the config sits in, not the working
// directory and not the paths on the command line. A copy of this file one level down
// matches nothing, reports every override as though it were absent, and gives no warning
// that it did.
//
// Why only `correctness`, with the rest named one by one: measured on this tree, over
// `src scripts test tools charcheck.config.ts`, each category *added* to the one above
// it — `correctness` 31 findings, `+suspicious` 172, `+pedantic` 1990, `+restriction`
// 6716, `+style` 23576. Past `correctness` these stop being latent bugs and become
// another linter's house style. Naming rules by hand costs a line each and buys a rule
// set this project actually agreed to.
{
  "$schema": "./node_modules/oxlint/configuration_schema.json",
  "plugins": ["eslint", "unicorn", "typescript", "oxc", "import"],
  "ignorePatterns": ["dist/**", "node_modules/**", "test/corpus/fixtures/**", ".tmp/**"],
  "categories": {"correctness": "error"},
  // No `"options": {"typeAware": true}` here, deliberately, even though the rules below need it.
  //
  // `options.typeAware` and the CLI's `--type-aware` are an OR, not an AND: either one on its
  // own turns the type-aware rules on (measured against a planted control — 4 findings under
  // config-only, flag-only, and both). oxlint has no `--no-type-aware` to switch them back off,
  // so setting it here would make *every* invocation type-aware, the pre-commit hook included.
  // That is not mainly a speed question (0.27 s to 0.64 s on a three-file commit, 0.31 s to
  // 1.27 s on the whole tree): a type-aware finding depends on files outside the staged set, so
  // a staged-scoped run would report differently from CI for reasons that are nothing to do
  // with the commit, and a hook that disagrees with CI is a hook people learn to skip.
  //
  // So `--type-aware` is passed by the `lint` package script and by verify's whole-tree gate,
  // and by nothing else. A bare `oxlint` in this repo runs the syntax rules only. Note the
  // failure mode this leaves: a type-aware rule that is not switched on reports nothing, which
  // reads exactly like a clean tree. If a count ever looks too good, plant the control from
  // docs/agent-correctness-playbook.md before believing it.
  "rules": {
    "eslint/no-case-declarations": "error",
    "eslint/no-empty": "error",
    "eslint/no-fallthrough": "error",
    "eslint/no-prototype-builtins": "error",
    "eslint/no-regex-spaces": "error",
    "eslint/no-useless-assignment": "error",
    "eslint/no-var": "error",
    "eslint/prefer-const": "error",
    "eslint/prefer-rest-params": "error",
    "eslint/prefer-spread": "error",
    "eslint/preserve-caught-error": "error",
    "eslint/no-console": "error",
    "typescript/ban-ts-comment": "error",
    "typescript/no-array-constructor": "error",
    "typescript/no-empty-object-type": "error",
    "typescript/no-explicit-any": "error",
    "typescript/no-namespace": "error",
    "typescript/no-require-imports": "error",
    "typescript/no-unnecessary-type-constraint": "error",
    "typescript/no-unsafe-function-type": "error",
    // On for production source at zero cost: all 565 `!` in this tree are in test files
    // (487 under test/corpus/, 78 in src/**/*.test.ts, none anywhere else). The override
    // below carries the same posture biome.json had, rather than conceding new ground.
    "typescript/no-non-null-assertion": "error",

    // ── Type-aware, from here down. Each needs `options.typeAware` above and `--type-aware`
    // on the command line. This first block is the half of `recommendedTypeChecked` a
    // syntax-only linter cannot replicate. Every one of them reports zero on non-test `src/`
    // as adopted, so the block is a gate against regression and not a cleanup backlog.
    "typescript/no-misused-promises": "error",
    "typescript/no-unsafe-argument": "error",
    "typescript/no-unsafe-assignment": "error",
    "typescript/no-unsafe-call": "error",
    "typescript/no-unsafe-enum-comparison": "error",
    "typescript/no-unsafe-member-access": "error",
    "typescript/no-unsafe-return": "error",
    "typescript/only-throw-error": "error",
    "typescript/prefer-promise-reject-errors": "error",
    "typescript/restrict-plus-operands": "error",
    "typescript/no-unnecessary-type-assertion": "error",
    "typescript/unbound-method": "error",

    // Reports uses of anything carrying a JSDoc `@deprecated`, reaching through `.d.ts` into
    // dependencies. Our own deprecations are visible in review; a dependency's are not, and
    // they are how a major-version break announces itself a release early. Zero tree-wide.
    "typescript/no-deprecated": "error",

    // A `void` expression read as a value is either a mistake or a claim the code does not
    // honour. Both options were measured: with them the rule costs nothing here, and without
    // `ignoreArrowShorthand` it would mean bracing every `forEach((x) => fn(x))` for no
    // correctness gain.
    "typescript/no-confusing-void-expression": [
      "error",
      {"ignoreArrowShorthand": true, "ignoreVoidOperator": true},
    ],

    // Not a style opinion about `return await`. This option fires only where a
    // `return somePromise` inside try/catch/finally would route the rejection past the
    // handler standing right there. Zero findings today.
    "typescript/return-await": ["error", "error-handling-correctness-only"],

    // `test()` and `describe()` from node:test return a promise nobody awaits, and that was
    // 1160 of the 1160 findings this rule produced before the option below. Naming them is
    // exactly what `allowForKnownSafeCalls` is for: the count goes to zero with no source
    // change. Do not "fix" a node:test call site to satisfy this rule.
    "typescript/no-floating-promises": [
      "error",
      {
        "allowForKnownSafeCalls": [
          {
            "from": "package",
            "package": "node:test",
            "name": [
              "test",
              "it",
              "describe",
              "suite",
              "before",
              "after",
              "beforeEach",
              "afterEach",
            ],
          },
        ],
      },
    ],
    // Declined, with the count: it fires on all 10 `new Array(...)` in this tree and every one
    // of them is `new Array(n).fill(v)`. The ambiguity the rule exists to catch — is that
    // argument a length or a lone element — cannot survive a `.fill` on the same expression, and
    // `Array.from({length: n}, () => v)` would be a slower way to say the same thing. The half
    // that would be a real defect, reading a hole as a value, `noUncheckedIndexedAccess` already
    // types as `T | undefined`.
    "unicorn/no-new-array": "off",

    // ── Considered and declined. Each of these was measured against this tree and read at the
    // site before being turned down, and each is listed here so the next sweep finds the
    // decision instead of re-deriving it. An unrecorded decline is worse than no decision.
    // Counts are non-test `src/**`, oxlint 1.80.0 / oxlint-tsgolint 7.0.2001.

    // 46. Forty-two of them are `boolean | undefined` in a conditional, where `if (x)` and
    // `if (x === true)` cannot differ. The remaining four are the class the rule exists for —
    // truthiness conflating `0` or `''` with absent — and every one of the four is deliberate:
    // `styles.ts` omits a zero-valued gradient degree and a zero inset because that is OOXML's
    // own default-omission rule, stated in the comment above `gradientFillXml`. The strongest
    // evidence is inside the one file: `styleAlignmentXml` already writes
    // `!== undefined && !== 0` for the numeric attributes and truthiness for the booleans, four
    // lines apart. The distinction the rule wants made is already made, by hand, where it
    // matters. Adopting would lengthen 42 lines that cannot change meaning, and would bury that
    // deliberate contrast in noise.
    "typescript/strict-boolean-expressions": "off",

    // 7. Every one is a runtime guard the types cannot enforce, in a library whose public API is
    // called from JavaScript where the types are erased: a `value is T` predicate that would
    // otherwise accept any `shareType`; an `undefined` skip over an array a caller supplied; a
    // `.filter` that compacts an array built by indexed assignment and so really can be sparse;
    // and the stream/filename check whose own comment says it backs up the type-level union
    // "rather than only the runtime check the constructor also carries". This package's job is
    // parsing files other tools wrote, and a type describing that input is our claim about it,
    // not a guarantee. Deleting a guard on the linter's word is how a malformed input turns from
    // a caught error into a crash.
    "typescript/no-unnecessary-condition": "off",

    // 50, and it is the closest thing to a mechanical gate for the architecture doc's rule that "an
    // enumerated attribute read from a file is admitted only through a type guard [...] rather than
    // cast in with `as`". It does not gate that: the rule holds today (every such token goes through
    // an `is*` guard, and the assertions that remain are not tokens at all), while 45 of the 50 are
    // `arr[i] as T` under `noUncheckedIndexedAccess`, which is the shape `no-non-null-assertion`
    // above forces by banning `x!`. Enabling it would demand suppressions on the escapes this config
    // already chose and still not distinguish a foreign token from an index. The guard rule stays
    // enforced by review, which is stated in `docs/architecture.md` next to the rule itself.
    "typescript/no-unsafe-type-assertion": "off",

    // 42. It asks for `x!` where a cast only removes null/undefined, which is exactly what
    // `no-non-null-assertion` above forbids. Two enabled rules cannot demand and ban the same
    // construct.
    "typescript/non-nullable-type-assertion-style": "off",

    // 3, all `switch (typeof value)` closing with `default: break` so the object shapes fall
    // through to the checks below. The unmatched cases the rule lists are `bigint | function |
    // object | symbol | undefined`; enumerating them would add five no-op cases to each switch.
    // The rule earns its place on a discriminated union, where a new variant should break the
    // build. `typeof`'s domain does not grow.
    "typescript/switch-exhaustiveness-check": "off",

    // Measured at 0 or 1 findings each. At that count a rule is neither a gate nor a cost, and
    // naming it only adds a line to keep true: no-unnecessary-boolean-literal-compare 0,
    // no-array-delete 0, use-unknown-in-catch-callback-variable 0,
    // no-unnecessary-template-expression 1, prefer-optional-chain 1, prefer-nullish-coalescing 1,
    // promise-function-async 2. Listed rather than enabled so a later sweep can skip them.
    // `no-misused-spread` used to be on this list and does not belong there: it is a `correctness`
    // rule, so it has been ON tree-wide all along. It caught five spreads over a string where the
    // format being encoded counts UTF-16 code units, which is the kind of thing this list is for
    // deciding is not worth catching.

    "import/no-duplicates": ["error", {"preferInline": true}],
    "import/no-cycle": "error",
    "import/no-self-import": "error",
    "import/named": "error",
    "import/default": "error",
    "import/namespace": "error",
  },
  // Overrides are applied in order and the last match wins, so these run narrow-to-narrower: the test
  // tree, then the corpus inside it, then the CLI trees, then the one file whose types are unstable.
  //
  // Read the note about tsgolint's tsconfig resolution in `test/tsconfig.json` before re-measuring any
  // count below. Until those per-directory configs existed, the type-aware rules judged every file
  // outside `src/**` under TypeScript's DEFAULTS while tsc judged it under the strict gate, and the
  // counts they produced were mostly that disagreement: `no-unnecessary-type-assertion` alone fell
  // from 301 to 22 the moment the linter could see `noUncheckedIndexedAccess`. Sixteen rules were off
  // across the whole harness on the strength of those numbers. Six are, now, and only where the
  // adapter's declared `any` boundary actually reaches.
  "overrides": [
    {
      "files": ["**/*.test.ts", "test/**/*.ts"],
      "rules": {
        // 565 across the tree: 487 under test/corpus/, 78 in src/**/*.test.ts, none anywhere else.
        // A test asserting on a value it has just constructed knows more than the checker does.
        "typescript/no-non-null-assertion": "off",
        // 7 — five in the corpus runner, two in the OOXML validation runner. Both print results.
        "eslint/no-console": "off",
      },
    },
    {
      // The corpus adapter's `any` boundary, and the only place in the tree that still has one. See
      // `test/corpus/untyped.ts` for the contract and ADR 0037 for why it stands. These six rules are
      // off because they report the boundary itself, not a defect: 2111 findings, every one of them
      // downstream of `Untyped`. The other ten rules that used to be listed here are back on — they
      // were measuring a tsconfig mismatch, not the contract.
      "files": ["test/corpus/**/*.ts"],
      "rules": {
        // 1, and it is the `Untyped = any` declaration itself.
        "typescript/no-explicit-any": "off",
        // 699, all `await api.someMethod()` in test/corpus/cases/** where the adapter method happens
        // to be synchronous. The cases await uniformly on purpose, so a method can become async
        // without touching 282 call sites. Deleting 699 awaits would trade that away for nothing.
        "typescript/await-thenable": "off",
        // 1412 between them — member-access 803, assignment 373, argument 122, call 66, return 48 —
        // and zero in production source. A corpus case describes behaviour against a deliberately
        // untyped surface, so the adapter is where `any` is SUPPOSED to live.
        "typescript/no-unsafe-argument": "off",
        "typescript/no-unsafe-assignment": "off",
        "typescript/no-unsafe-call": "off",
        "typescript/no-unsafe-member-access": "off",
        "typescript/no-unsafe-return": "off",
      },
    },
    {
      // CLI entry points: printing is what they are for. 47 findings, no defects. tools/ is not in
      // this list — it reports zero, writing through `process.stdout` instead — so the rule stays on
      // there and a future `console.log` in a tool has to be a decision rather than a habit. The two
      // site scripts are named one by one rather than as `www/scripts/**`, because the other two
      // files in that directory are libraries the site config imports, and a report from one of them
      // would be a defect rather than output.
      "files": [
        "scripts/**/*.ts",
        "www/scripts/check.ts",
        "www/scripts/check-samples.ts",
        "www/scripts/sync-docs.ts",
        "charcheck.config.ts",
      ],
      "rules": {"eslint/no-console": "off"},
    },
    {
      // The one file the type-aware rules cannot judge, and it is not this file's fault. smoke-dist
      // imports the package by NAME, through the published `exports` map, so its types live in dist/
      // — which does not exist before a build and never in the corpus CI job. tsconfig.test.json
      // excludes it for that reason, which leaves tsgolint no project for it, which makes every
      // imported symbol `any`: 37 findings that say nothing about the file and change with whether
      // someone happened to build first. A gate whose verdict depends on that is not a gate.
      // tsconfig.dist.json type-checks this file properly, after a build, in CI.
      "files": ["scripts/smoke-dist.ts"],
      "rules": {
        "typescript/await-thenable": "off",
        "typescript/no-unsafe-argument": "off",
        "typescript/no-unsafe-assignment": "off",
        "typescript/no-unsafe-call": "off",
        "typescript/no-unsafe-member-access": "off",
        "typescript/no-unsafe-return": "off",
      },
    },
  ],
}
