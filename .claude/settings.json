{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; case \"$f\" in */src/*.ts|src/*.ts) out=$(npm run typecheck 2>&1 && npm run test:src 2>&1) || { echo \"$out\" >&2; exit 2; } ;; esac; }",
            "statusMessage": "Running gates (typecheck + test:src)",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
