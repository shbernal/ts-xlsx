{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -z \"$(git status --porcelain -- src 2>/dev/null)\" ] && exit 0; out=$(npm run typecheck 2>&1 && npm run test:src 2>&1) || { echo \"$out\" >&2; exit 2; }",
            "statusMessage": "Running gates (typecheck + test:src)",
            "timeout": 120
          }
        ]
      }
    ]
  }
}
